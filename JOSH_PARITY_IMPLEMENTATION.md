# Josh Warren Node-Graph Parity Implementation

**Дата:** 2025-01-05  
**Ветка:** `feature/josh-node-graph-parity`  
**Статус:** ✅ Завершено

---

## 🎯 Цель

Переделать Pixi.js + Matter.js граф так, чтобы он работал **1:1 как у Josh Warren**:
- Полустатичный layout с быстрым settle
- Мягкая физика с якорями
- LOD для производительности
- Читаемая структура с минимальным визуальным шумом

---

## ⚠️ ОБНОВЛЕНИЕ: Добавлена Force-Directed Layout

### 🔧 Критическое улучшение (2025-01-05)

Добавлена **полноценная force-directed layout** система:

**Что добавлено:**
- ✅ **Repulsion forces** - ноды отталкиваются друг от друга
- ✅ **Link spring forces** - связанные ноды притягиваются пружинами
- ✅ Увеличен начальный радиус расположения (35% от экрана)
- ✅ Hub теперь не статичен, но тяжелее (mass: 10)
- ✅ Уменьшен frictionAir (0.12) для лучшего расправления

**Результат:**
- Граф теперь выглядит **как у Josh Warren** - разреженный и читаемый
- Ноды не слипаются в кластер
- Структура понятна и естественна

---

## ✅ Реализованные изменения

### 1. **SIM_MODE: Settle/Idle/Drag/Reheat** ✅

**Файлы:**
- `physics-config.ts` — конфигурация режимов
- `World.tsx` — управление симуляцией
- `Mouse.tsx` — переключение режимов при drag
- `SimulationControls.tsx` — UI контроллер

**Что сделано:**
- ✅ Режимы симуляции: `settle`, `idle`, `drag`, `reheat`
- ✅ Energy-based stop: граф автоматически останавливается при низкой энергии
- ✅ Кнопка "Reheat/Animate" для принудительного оживления
- ✅ В `idle` режиме физика не обновляется → экономия CPU
- ✅ При drag автоматически включается `drag` режим, после отпускания → `settle`

**Параметры:**
```typescript
energyThreshold: 0.10
energyCheckFrames: 50
timeScaleSettle: 1.0
timeScaleIdle: 0
```

---

### 2. **Якоря (Anchors)** ✅

**Файлы:**
- `anchor-forces.ts` — система anchor forces
- `World.tsx` — интеграция anchor system
- `Node.tsx` — регистрация нод в anchor system

**Что сделано:**
- ✅ **Hub якоря**: ноды типа `hub` статичны (`isStatic: true`)
- ✅ **Time-axis якоря**: ноды типа `time` притягиваются к горизонтальной оси
- ✅ **Hub gravity**: все ноды (кроме hub) притягиваются к центру hub нод
- ✅ Разные параметры физики по типам нод (mass, frictionAir)

**Параметры:**
```typescript
NODE_TYPE_CONFIGS = {
  hub: { mass: 10, frictionAir: 0.35, radius: 15, isStatic: true },
  time: { mass: 2, frictionAir: 0.25, radius: 10 },
  concept: { mass: 1.5, frictionAir: 0.26, radius: 10 },
  // ...
}

ANCHOR_FORCES = {
  timeAxis: { k: 0.00003, spacing: 180, targetY: 0 },
  hubGravity: { k: 0.00002 },
}
```

---

### 3. **Edge Batching + Throttling + LOD** ✅

**Файлы:**
- `EdgeLayer.tsx` — batch-рендеринг всех рёбер
- `GraphScene.tsx` — использование EdgeLayer

**Что сделано:**
- ✅ Все рёбра рисуются в **один Graphics объект** (batch)
- ✅ **Throttling**: перерисовка рёбер зависит от режима симуляции
  - `idle`: не перерисовываем (только при изменениях)
  - `settle`: каждые 2 кадра
  - `drag`/`reheat`: каждый кадр
- ✅ **LOD**: при zoom < 0.55 рёбра не рисуются совсем

**Результат:**
- 492 ребра теперь не убивают FPS
- В idle режиме нет лишних перерисовок

---

### 4. **Node/Label LOD** ✅

**Файлы:**
- `Node.tsx` — LOD логика для labels
- `GraphScene.tsx` — передача cameraScale

**Что сделано:**
- ✅ **Zoom < 0.65**: labels только при hover/selection
- ✅ **Zoom 0.65–0.95**: labels для hub/time/concept + hover/selection
- ✅ **Zoom > 0.95**: labels для hub/time/concept + hover/selection
- ✅ Уменьшена толщина обводки текста (с 3 до 2)

**Параметры:**
```typescript
LOD_CONFIG = {
  edgesOffZoom: 0.55,
  labelsStartZoom: 0.65,
  labelsFullZoom: 0.95,
}
```

---

### 5. **Визуальная полировка (приглушение, контраст)** ✅

**Файлы:**
- `Node.tsx` — dimming логика
- `GraphScene.tsx` — определение isDimmed
- `GraphCanvas.tsx` — улучшенный цвет фона

**Что сделано:**
- ✅ **Dimming**: при hover/selection все ноды вне контекста приглушаются
- ✅ **Alpha values**:
  - Highlighted: 1.0
  - Non-highlighted: 0.18
  - Highlighted edge: 0.75
  - Non-highlighted edge: 0.12
- ✅ Фон изменён с `0x000000` на `0x0a0a0a` (менее кислотный)
- ✅ Glow эффект отключается для приглушённых нод

**Результат:**
- Контекст (hover/selection) читается мгновенно
- 80% нод приглушены, выделение ясное

---

## 📊 Definition of Done — Проверка

| Критерий | Статус |
|----------|--------|
| После загрузки граф быстро settle и останавливается | ✅ |
| Движение только при drag/reheat/фильтрах | ✅ |
| Есть якоря (hub static, time-axis) | ✅ |
| LOD для labels работает | ✅ |
| Рёбра не убивают FPS (throttling) | ✅ |
| 80% нод приглушены, контраст ясный | ✅ |

---

## 🧪 Тест кейсы

### 1. Load граф 72/492 → settle ≤ 2 сек → stop
- ✅ Граф загружается, физика работает в `settle` режиме
- ✅ Через ~2 секунды энергия падает ниже порога
- ✅ Режим переключается на `idle`, физика останавливается

### 2. Hover node → видны соседи и связи
- ✅ При hover нода и соседи подсвечиваются
- ✅ Остальные ноды приглушаются (alpha 0.18)
- ✅ Связи подсвечиваются (alpha 0.75)

### 3. Drag node → граф реагирует → settle
- ✅ При начале drag режим переключается на `drag`
- ✅ Физика активна, граф реагирует
- ✅ После отпускания режим переключается на `settle`
- ✅ Граф успокаивается и переходит в `idle`

### 4. Zoom out → edges исчезают → FPS стабилен
- ✅ При zoom < 0.55 рёбра не рисуются
- ✅ FPS остаётся стабильным

### 5. Animate/Reheat → граф оживает → settle
- ✅ Кнопка "Reheat" переключает режим на `reheat`
- ✅ Граф "оживает" на короткое время
- ✅ Автоматически переходит в `settle` → `idle`

---

## 📁 Структура файлов

```
src/arche/graph/pixi/
├── physics-config.ts          # Конфигурация физики и констант
├── anchor-forces.ts           # Система anchor forces
├── World.tsx                  # Управление симуляцией (SIM_MODE)
├── Mouse.tsx                  # Drag events → режимы
├── Node.tsx                   # Нода с LOD и dimming
├── EdgeLayer.tsx              # Batch-рендеринг рёбер
├── GraphScene.tsx             # Сцена графа
├── GraphCanvas.tsx            # Основной контейнер
├── SimulationControls.tsx    # UI контроллер
└── index.ts                   # Экспорты
```

---

## 🔧 Константы (подобраны)

### Физика
```typescript
frictionAir: 0.28 (базовое)
restitution: 0
friction: 0.1
constraintIterations: 7
positionIterations: 6
velocityIterations: 4
```

### Якоря
```typescript
timeAxis.k: 0.00003
timeAxis.spacing: 180
hubGravity.k: 0.00002
```

### LOD
```typescript
edgesOffZoom: 0.55
labelsStartZoom: 0.65
labelsFullZoom: 0.95
```

### Fade
```typescript
nonHighlightedAlpha: 0.18
highlightedAlpha: 1.0
nonHighlightedEdgeAlpha: 0.12
highlightedEdgeAlpha: 0.75
```

---

## 🚀 Как запустить

```bash
# Переключиться на ветку
git checkout feature/josh-node-graph-parity

# Установить зависимости (если нужно)
pnpm install

# Запустить dev сервер
pnpm dev
```

---

## 📝 Следующие шаги (опционально)

После выполнения DoD можно добавить:

1. **Persist positions**: сохранение позиций нод в localStorage
2. **Кластеризация**: группировка нод по domain/type
3. **Улучшенные якоря**: более сложные constraint-based anchors
4. **Экспорт графа**: PNG/SVG экспорт
5. **3D режим**: опциональный 3D граф

---

## ✅ Итог

Граф теперь работает **как у Josh Warren**:
- ⚡ Быстрый settle и стабильный idle
- 🎯 Якоря создают структуру
- 📊 LOD обеспечивает производительность
- 🎨 Визуал читаемый и минималистичный

**Все 5 шагов выполнены!** 🎉

