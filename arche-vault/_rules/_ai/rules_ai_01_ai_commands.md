---
group: rules
---

#rules

# 🧰 AI Commands — команды для ИИ-редактора и режим "согласование → выполнение"

Этот файл определяет набор команд для ИИ,
который помогает оформлять и поддерживать базу знаний.

Ключевое правило:
ИИ НЕ вносит изменения сразу.
Любой запрос выполняется в 2 этапа:

Этап 1 — PLAN/DRAFT:
ИИ отвечает планом действий или коротким драфтом результата.
Пользователь даёт апрув.

Этап 2 — EXECUTE:
ИИ выполняет работу строго в рамках апрува.

Если апрува нет — никаких изменений.

---

## 0. Общий протокол ответа

Любая команда всегда возвращает:

1) Summary — что будет сделано (1–5 строк)
2) Scope — какие файлы/папки затрагиваются
3) Changes — какие типы изменений планируются (структура, стиль, ссылки, мета)
4) Risks — где возможны сомнения/конфликты
5) Output Preview — пример 1–2 фрагментов (если уместно)
6) Approval Request — короткая просьба "Апрув/Правки"

Только после апрува запускается EXECUTE.

---

## 1. Команды форматирования и стилистики

### /format
Назначение: привести выбранные файлы к шаблону и стилю.
Пример:
- /format path="/04_Events" mode="readability"

Опции:
- mode="readability" (упрощение языка, короткие абзацы)
- mode="structure" (разделы по шаблону, без переписывания смысла)
- mode="light" (минимальные правки, пунктуация/заголовки)

Запрещено:
- менять факты
- добавлять интерпретации без отдельного указания
- менять meta-блок кроме updated

---

### /normalize-meta
Назначение: проверить и выровнять meta-блоки по стандарту.
Пример:
- /normalize-meta path="/03_Concepts"

Результат:
- список несоответствий
- план исправлений
- после апрува — исправления

---

## 2. Команды проверки связей

### /audit-links
Назначение: проверить ссылки:
- битые/потерянные
- "красные" (не созданные) — как список задач
- сиротские заметки (без входящих/исходящих)

Пример:
- /audit-links path="/"

Вывод (PLAN):
- отчёт по категориям
- топ-10 кандидатов на новые связи
- список заметок-сирот

EXECUTE (только после апрува):
- может добавлять предложения ссылок в конец заметок в разделе "Связанные заметки"
- НЕ создаёт новые заметки без команды /create

---

### /suggest-links
Назначение: предложить недостающие связи для конкретного файла.
Пример:
- /suggest-links file="Первая мировая война"

Вывод:
- 10–20 предложений ссылок
- объяснение в 1 строку почему связь уместна
- без применения, пока пользователь не выберет

---

## 3. Команды создания и обновления заметок

### /create
Назначение: создать новую заметку-атом по шаблону (seed уровень).
Пример:
- /create type="event" title="Первая мировая война" domain="[history, politics]"

PLAN:
- черновой skeleton заметки
- список первичных ссылок (5–10)
- запрос апрува

EXECUTE:
- создаёт файл только после апрува

---

### /update-knowledge
Назначение: предложить улучшение существующей заметки.
Пример:
- /update-knowledge file="Первая мировая война" focus="interpretations"

PLAN:
- что именно будет добавлено/переписано
- 1–2 примерных абзаца
- список ссылок, которые будут добавлены

EXECUTE:
- применяет только согласованные изменения

---

## 4. Режим "только через источники"

### /source-only-create
Назначение: создать заметку только на базе предоставленных источников.
Источники = текст, ссылки, выписки, PDF, заметки пользователя.

Пример:
- /source-only-create type="event" title="Первая мировая война"
  sources="[doc1, doc2]" (пользователь прикладывает)

PLAN:
- структура заметки
- какие утверждения откуда
- какие места остаются "не покрыты источниками"

EXECUTE:
- ИИ пишет только то, что подтверждено источниками
- если утверждение не подтверждено — оно либо не пишется, либо помечается как "не подтверждено источниками"

Правило:
- никаких "я знаю, что..." без источника
- допускаются нейтральные формулировки неопределённости

---

### /source-only-update
Назначение: обновить существующую заметку только из источников.
Пример:
- /source-only-update file="Распад СССР" sources="[...]" focus="causes"

---

## 5. Команды для спорных тем

### /add-interpretations
Назначение: добавить раздел "Интерпретации" по правилам rules_08_interpretations.
Пример:
- /add-interpretations file="Распад СССР" min=2

PLAN:
- 2–4 интерпретации (название рамки + 2 аргумента + 3 ссылки)
- вопросы для пользователя: что ближе/что добавить

EXECUTE:
- вставляет блок в заметку после апрува

---

## 6. Команды обзоров и навигации

### /build-hub
Назначение: создать или обновить HUB-страницу по выбранной теме.
Пример:
- /build-hub title="XX век" focus="[events, ideas, works]"

PLAN:
- структура HUB
- список ссылок, которые попадут внутрь

EXECUTE:
- создаёт/обновляет HUB после апрува

---

### /weekly-review
Назначение: составить "план следующей недели" по базе:
- что сиротское
- что выросло из seed
- какие связи добавить

PLAN:
- список задач и приоритетов
EXECUTE:
- не делает правки, только выдаёт план

---

## 7. Дополнительные команды (предложенные варианты)

### /extract-atoms
Назначение: из одного большого текста выделить атомы и предложить разбиение.
Пример:
- /extract-atoms from="paste" types="[person, event, concept]"

PLAN:
- список будущих файлов
- черновые заголовки и первые ссылки

---

### /merge-duplicates
Назначение: найти потенциальные дубликаты (разные имена, один смысл).
PLAN:
- пары-кандидаты + причины
EXECUTE:
- только после апрува и только если пользователь выбрал пару

---

### /rename-safe
Назначение: безопасно переименовать заметку, сохранив связи.
PLAN:
- новый title, какие ссылки будут затронуты
EXECUTE:
- после апрува

---

### /quality-check
Назначение: проверить заметку на:
- атомарность
- нейтральность
- наличие минимум 2 ссылок
- корректность разделов
PLAN:
- список проблем + предложения исправлений
EXECUTE:
- только после апрува

---

## 8. Приоритеты поведения ИИ

1) Не навреди структуре (meta, ссылки, атомарность)
2) Не навреди смыслу (без добавления "истины")
3) Улучшай читаемость
4) Предлагай связи и вопросы
5) Изменения только после апрува

---

## 9. Формат апрува

Пользователь отвечает одним из:
- "Апрув" / "Approve"
- "Апрув с правками: ..."
- "Не апрув, измени план так: ..."

Без апрува — только обсуждение.
