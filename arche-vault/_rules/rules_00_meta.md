---
group: rules
---

#rules

# 🧬 Meta System — как устроена структура знаний

Этот файл описывает, как и зачем используется meta-блок
в каждой заметке проекта.

Meta-блок — это описание заметки как объекта системы.
Он не управляет содержимым и не мешает письму.

---

## 1. Общий вид meta-блока

Каждый файл-атом начинается с meta-блока:

```yaml
id: <id>
type: <type>
domain: [<domain>, ...]
status: <status>
group:
created: <date>
updated:
```

Если значение неизвестно — поле оставляется пустым,
но не удаляется.

---

## 2. Поле id — идентификатор

id — стабильный идентификатор заметки.
Имя файла может меняться, id — нет.

### Допустимы ДВА режима id

### Режим A (основной, рекомендованный)
UUID (через шаблоны / плагины).

Пример:
id: 9f3c2a8e-1b7a-4f3a-9c6e-2c9e3b9a1a12

Используется для:
- основной базы знаний
- сайта
- автоматизации
- AI-инструментов

### Режим B (допустим для демо и примеров)
Человекочитаемые id с префиксами.

Примеры:
ex-001
tpl-003
imp-2024-01-15-01

Используется ТОЛЬКО для:
- примеров
- шаблонов
- импортированных наборов

---

## 3. Поле type — тип сущности

type отвечает на вопрос:
ЧЕМ является эта заметка?

Допустимые значения:

```
hub      — навигационная страница  
time     — эпоха / период  
person   — человек  
concept  — идея / понятие  
event    — событие  
work     — произведение  
place    — место / география  
science  — научная теория / модель  
note     — личная мысль  
```

type — это онтология, а не папка.
Папки можно менять, type — нет.

---

## 4. Поле domain — область знания

domain отвечает на вопрос:
К какой области (или областям) относится заметка?

Можно указывать несколько значений.

Рекомендуемый базовый набор:
```
history  
philosophy  
art  
literature  
religion  
science  
politics  
culture  
geography  
```

domain не является строгой классификацией.
Лучше указать меньше, чем больше.

---

## 5. Поле status — зрелость понимания

status отражает текущее состояние понимания.

Допустимые значения:

```
seed        — идея зафиксирована  
growing     — начинается понимание  
stable      — базовое понимание есть  
deep        — хорошо проработано  
question    — есть конфликт или сомнение
```

Статус можно менять в обе стороны.
Это навигация, а не оценка.

---

## 6. Поле group — системная группировка (ОПЦИОНАЛЬНО)

group используется для логической группировки файлов,
НЕ для смысловой классификации.

Допустимые примеры использования:

```
example     — демонстрационные файлы  
template    — шаблоны  
import      — импортированные материалы  
draft       — временные рабочие файлы  
archive     — выведенные из активного использования  
```

Правила:
- group не обязателен
- group не дублируется тегами
- group не используется для тем или смыслов

---

## 7. Поля created и updated

created:
- дата создания заметки
- не меняется

updated:
- заполняется ТОЛЬКО при существенных изменениях

Существенные изменения:
- изменение структуры разделов
- добавление / переработка интерпретаций
- добавление 3+ значимых связей
- существенное переписывание сути заметки

Опечатки и мелкие правки updated не требуют.

---

## 8. Что meta НЕ делает

Meta-блок:
- не управляет текстом
- не диктует стиль
- не заменяет ссылки
- не требует идеального заполнения

---

## 9. Базовый принцип

Если meta мешает писать —
значит, meta используется неправильно.

Смысл — в тексте  
Связи — в ссылках  
Структура — в meta
