# Museum Redesign — Руководство по Внедрению

## ✅ Что Реализовано

### 1. Дизайн-Система

**CSS Variables (src/index.css):**
- Музейная палитра: черный/графит фон (oklch 0.08-0.20)
- Тонкие линии и границы (opacity 0.3)
- Минимальные цветовые акценты для типов
- Зерно (grain texture) через CSS overlay

**Типографика:**
- Serif для заголовков (Georgia, Times New Roman)
- Sans для интерфейса (Inter, Segoe UI)
- Крупные размеры (h1: 3.5rem, h2: 2.5rem)
- Улучшенный кернинг и рендеринг

### 2. Компоненты Museum

**Создано в `src/components/museum/`:**

```
museum/
├── GrainBackground.tsx    # Зерно с настраиваемой интенсивностью
├── MuseumCard.tsx         # Карточка (default/elevated/subtle)
├── MuseumLayout.tsx       # Layout-обёртка с зерном
├── MuseumNavigation.tsx   # Навигация в музейном стиле
├── Timeline.tsx           # Временная шкала с эпохами
├── TypeBadge.tsx          # Бейджи типов (person/work/concept...)
└── index.ts               # Экспорты
```

**TypeBadge:**
- Минимальные акценты для person/concept/work/time/place
- Rounded pill design
- Прозрачные границы

**MuseumCard:**
- 3 варианта: default, elevated, subtle
- Hover эффекты
- Тонкие границы

**Timeline:**
- Вертикальный скролл по эпохам
- Группировка заметок по периодам
- Sticky headers для эпох
- Автопарсинг дат из markdown

### 3. Роутинг

**Установлен react-router-dom:**
```
npm install react-router-dom
npm install -D @types/react-router-dom
```

**Структура:**
```
/                 → HomePage (главная с группировкой по типам)
/timeline         → TimelinePage (хронологический просмотр)
/note/:noteId     → NotePage (просмотр отдельной заметки)
/graph            → GraphPage (граф-визуализация)
```

**Компоненты:**
- `RootLayout` — общий layout с навигацией и sidebar
- `MuseumNavigation` — верхняя навигация
- Все страницы в `src/pages/`

### 4. Страницы

**HomePage (`src/pages/HomePage.tsx`):**
- Группировка заметок по типам (hub, time, concept, person, work, place, note)
- Grid layout с карточками
- "Показать все" для типов с >9 заметками

**TimelinePage (`src/pages/TimelinePage.tsx`):**
- Вертикальная временная ось
- Эпохи: Античность, Средневековье, Возрождение, Новое время...
- Автоматическое распределение заметок по эпохам
- Sticky ruler с метками веков

**NotePage (`src/pages/NotePage.tsx`):**
- Крупная типографика для заголовка
- Markdown контент с улучшенным рендерингом
- Раздел "Связанные заметки" внизу
- Breadcrumb навигация (кнопка "Назад")

**GraphPage (`src/pages/GraphPage.tsx`):**
- Интеграция существующего GraphView
- Без зерна (для производительности)
- Full-height viewport

### 5. Интеграция

**App.tsx:**
- Загрузка заметок при старте
- Применение темы
- Красивый loader с анимацией
- RouterProvider вместо прямого Dashboard

**RootLayout:**
- MuseumNavigation + ArcheSidebar
- Outlet для роутера
- Фиксированная навигация (73px padding-top)

---

## 🚀 Как Запустить

```bash
# 1. Установить зависимости (если ещё не)
pnpm install

# 2. Запустить dev сервер
pnpm dev

# 3. Открыть браузер
http://localhost:5173
```

**Навигация:**
- `/` — главная страница
- `/timeline` — таймлайн
- `/graph` — граф
- `/note/person-aristotle-001` — просмотр заметки

---

## 📦 Структура Проекта

```
src/
├── App.tsx                 # ✅ ОБНОВЛЕНО: роутер + загрузка
├── index.css               # ✅ ОБНОВЛЕНО: музейная палитра + типографика
├── routes/
│   └── index.tsx           # ✅ НОВОЕ: определение роутов
├── layouts/
│   └── RootLayout.tsx      # ✅ НОВОЕ: общий layout
├── pages/
│   ├── HomePage.tsx        # ✅ НОВОЕ: главная
│   ├── TimelinePage.tsx    # ✅ НОВОЕ: таймлайн
│   ├── NotePage.tsx        # ✅ НОВОЕ: страница заметки
│   └── GraphPage.tsx       # ✅ НОВОЕ: граф
├── components/
│   └── museum/             # ✅ НОВОЕ: музейные компоненты
│       ├── GrainBackground.tsx
│       ├── MuseumCard.tsx
│       ├── MuseumLayout.tsx
│       ├── MuseumNavigation.tsx
│       ├── Timeline.tsx
│       ├── TypeBadge.tsx
│       └── index.ts
├── arche/
│   ├── ui/                 # Старые компоненты (Dashboard, Sidebar, Tabs)
│   ├── graph/              # GraphView (без изменений)
│   ├── markdown/           # MarkdownViewer (без изменений)
│   ├── parser/             # Парсер заметок (без изменений)
│   └── state/              # Zustand store (без изменений)
└── docs/
    ├── TIME_RULER_DESIGN.md          # ✅ НОВОЕ: детальный дизайн Time Ruler
    └── REDESIGN_IMPLEMENTATION.md    # ✅ НОВОЕ: это руководство
```

---

## 🎨 Визуальные Изменения

### До
- Светлые/тёмные темы (обычные серые)
- Стандартная типографика
- Базовые карточки
- Sidebar + Tabs навигация

### После
- Музейная палитра (черный/графит)
- Serif заголовки, крупные размеры
- Зерно (grain) для текстуры
- Топ-навигация + роутинг
- TypeBadge с акцентами

---

## 🔧 Кастомизация

### Изменение Цветов Типов

`src/components/museum/TypeBadge.tsx`:
```typescript
const TYPE_COLORS: Record<string, string> = {
  person: 'text-[var(--type-person)] border-[var(--type-person)]/30',
  // ... добавить свои типы
};
```

`src/index.css`:
```css
:root {
  --type-person: oklch(0.75 0.05 240); /* синий */
  --type-custom: oklch(0.70 0.05 180); /* ваш цвет */
}
```

### Настройка Зерна

```tsx
<GrainBackground intensity="subtle" | "medium" | "strong">
  {children}
</GrainBackground>
```

### Добавление Эпох в Timeline

`src/components/museum/Timeline.tsx`:
```typescript
const eras: TimelineEra[] = [
  { name: 'Античность', startYear: -800, endYear: 500, notes: [] },
  // добавить свои эпохи
  { name: 'Ваша Эпоха', startYear: 2000, endYear: 2025, notes: [] },
];
```

---

## 🔄 Миграция Старого Кода

### Dashboard.tsx
**Статус:** Сохранён, но не используется в основном роутинге

**Что сделать:**
- Можно удалить или оставить для референса
- Вся функциональность перенесена в новые страницы

### Sidebar & Tabs
**Статус:** Sidebar используется в RootLayout

**Tabs:** Не используются (заменены роутингом)

### GraphView
**Статус:** Интегрирован в GraphPage без изменений

### MarkdownViewer
**Статус:** Используется в NotePage без изменений

---

## 📊 Performance

### Виртуализация Timeline
- Рендерит только видимые карточки
- Sticky positioning для эпох
- RequestAnimationFrame для скролла

### Lazy Loading
- Страницы загружаются по требованию (code splitting)
- Изображения в markdown через `react-markdown`

### CSS Optimizations
- Will-change для анимируемых элементов
- Transform вместо top/left
- Backdrop-blur только на nav

---

## 🐛 Известные Вопросы

### 1. Parsing Дат
**Проблема:** Автоматический парсинг дат может быть неточным

**Решение:**
- Добавить frontmatter поля:
```yaml
---
timeline:
  start_year: 1900
  end_year: 1950
  importance: 0.8
---
```

### 2. Много Заметок без Дат
**Проблема:** Timeline пуст, если заметки без временных меток

**Решение:**
- Ручное добавление дат в frontmatter
- Или отображение заметок в "Неопределённый период"

### 3. Sidebar на Мобильных
**Проблема:** Sidebar может не помещаться

**Решение:**
- Уже настроена адаптивность (скрывается на md:)
- Кнопка меню в навигации

---

## 🎯 Следующие Шаги

### Фаза 1 (Завершено) ✅
- [x] Дизайн-система
- [x] Музейные компоненты
- [x] Роутинг
- [x] Основные страницы
- [x] Timeline базовый

### Фаза 2 (Следующая)
- [ ] Реализация Time Ruler (по дизайну из `TIME_RULER_DESIGN.md`)
- [ ] LOD (Level of Detail) для Timeline
- [ ] Snap points к эпохам
- [ ] Mini-map навигация
- [ ] Фильтры на таймлайне

### Фаза 3 (Будущее)
- [ ] Focus mode
- [ ] Связи между карточками
- [ ] Параллакс эффекты
- [ ] Анимированные переходы
- [ ] Canvas/WebGL оптимизации

---

## 🤝 Как Использовать Старый Dashboard

Если нужно временно вернуться к старому интерфейсу:

```tsx
// src/App.tsx
import { Dashboard } from './arche/ui/Dashboard';

function App() {
  // ... загрузка заметок
  
  return <Dashboard />; // вместо <RouterProvider router={router} />
}
```

Или добавить роут:
```tsx
// src/routes/index.tsx
{
  path: '/legacy',
  element: <Dashboard />,
}
```

---

## 📝 Заметки для Разработчиков

### CSS Variables
Все переменные доступны через `var(--name)`:
- `var(--background)` — фон
- `var(--foreground)` — текст
- `var(--border)` — границы
- `var(--type-person)` — акцент для person

### Zustand Store
Без изменений. Используйте:
```tsx
const notes = useArcheStore(state => state.notes);
const openNote = useArcheStore(state => state.openNote);
```

### Навигация
```tsx
import { useNavigate } from 'react-router-dom';

const navigate = useNavigate();
navigate('/note/' + noteId);
navigate(-1); // назад
```

### TypeScript
Все компоненты типизированы. Проверяйте через:
```bash
pnpm tsc --noEmit
```

---

## 📚 Документация

- **TIME_RULER_DESIGN.md** — детальный дизайн Time Ruler с алгоритмами
- **REDESIGN_IMPLEMENTATION.md** — это руководство
- **README.md** — общая информация о проекте

---

## ✨ Готово к Использованию!

Запустите `pnpm dev` и наслаждайтесь музейным интерфейсом.

**Полезные команды:**
```bash
pnpm dev          # запуск dev сервера
pnpm build        # production build
pnpm preview      # просмотр production build
pnpm lint         # проверка кода
```


