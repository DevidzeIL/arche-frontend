# Focus Mode — Руководство

## ✨ Что Реализовано

### Focus Mode для Time Ruler

**Полная реализация интерактивности:**
- ✅ Focus Mode (затемнение + центрирование)
- ✅ Связи между карточками (линии при hover/focus)
- ✅ Фильтры по доменам (философия/искусство/литература/наука)
- ✅ Плавные transitions для всех эффектов
- ✅ Keyboard shortcuts (Escape для выхода)
- ✅ Hover highlights для связанных заметок

---

## 🎯 Focus Mode

### Как Активировать

**Клик по карточке:**
1. Первый клик → **активирует Focus Mode**
   - Карточка центрируется и увеличивается (scale 1.1)
   - Остальные карточки затемняются (opacity 0.25) и размываются (blur 2px)
   - Связанные заметки подсвечиваются (ring + увеличение)
   - Фон затемняется (backdrop-blur overlay)
   - Появляются линии связей (dashed, animated)

2. Повторный клик → **открывает заметку** и выходит из Focus Mode

### Как Выйти из Focus Mode

**3 способа:**
1. **Escape** — нажать клавишу
2. **Клик на фон** — кликнуть за пределами карточек
3. **Повторный клик** — кликнуть на focused карточку снова

---

## 🔗 Связи Между Карточками

### Hover Highlights

**Hover на карточку (без Focus Mode):**
- Карточка увеличивается (scale 1.05)
- Связанные заметки подсвечиваются (ring 1px primary/40)
- Все остальные затемняются (opacity 0.25)

**Работает через:**
- Анализ поля `links` в заметке
- Поиск связанных по названиям
- Динамическое создание `relatedNoteIds` Set

### Connection Lines

**Визуализация связей (только в Focus Mode):**
- Линии рисуются от focused карточки к связанным
- Dashed gradient линии (primary цвет)
- Анимация `pulse` для живого эффекта
- SVG overlay с `z-index: 15`

**Технология:**
- Компонент `ConnectionLines.tsx`
- SVG `<line>` элементы
- Gradient fill для fade эффекта
- Координаты вычисляются через `yearToPixel()`

---

## 🔍 Фильтры по Доменам

### Новые Фильтры

**6 доменов:**
- 🧠 Философия (philosophy)
- 🎨 Искусство (art)
- 📚 Литература (literature)
- 🔬 Наука (science)
- 📜 История (history)
- 🧬 Психология (psychology)

### Использование

**UI:**
- Вторая строка в `TimelineFilters`
- Pill buttons (как типы)
- По умолчанию все активны

**Логика:**
- Если фильтр активен → показываем заметки с этим доменом
- Если все фильтры неактивны → показываем все
- Работает через `note.domain` массив

**Сброс:**
- Кнопка "Сбросить фильтры" (появляется если есть активные)
- Сбрасывает типы + домены

---

## 🎨 Transitions & Animations

### Плавные Переходы

**Все эффекты используют CSS transitions:**

```css
transition-all duration-300 ease-out
```

**Что анимируется:**
- `transform: scale()` — увеличение карточек
- `opacity` — затемнение/осветление
- `filter: blur()` — размытие dimmed карточек
- `ring` — подсветка границ
- `shadow` — тени при focus

### Анимации

**Pulse для линий связей:**
```css
animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite
```

**Backdrop blur для overlay:**
```css
backdrop-blur-sm (4px blur)
```

---

## 🔧 Технические Детали

### Состояние

**3 новых state переменных:**
```typescript
const [focusedNoteId, setFocusedNoteId] = useState<string | null>(null);
const [hoveredNoteId, setHoveredNoteId] = useState<string | null>(null);
const [focusMode, setFocusMode] = useState(false);
```

**Вычисляемое:**
```typescript
const relatedNoteIds = useMemo(() => {
  const targetId = focusedNoteId || hoveredNoteId;
  // ... находим связанные через note.links
  return new Set<string>();
}, [focusedNoteId, hoveredNoteId, timelineNotes]);
```

### Компоненты

**Обновлённые:**
1. `TimeRuler.tsx` — главная логика, overlay, keyboard
2. `TimelineCard.tsx` — новые props (isHovered, isRelated, focusMode)
3. `TimelineFilters.tsx` — домены, сброс фильтров
4. `MuseumCard.tsx` — вариант `elevated`

**Новые:**
5. `ConnectionLines.tsx` — SVG линии связей

### MuseumCard Variants

**4 варианта:**
- `default` — обычная карточка
- `elevated` — focused (border primary, shadow-xl)
- `subtle` — приглушённая (bg-card/50)
- `ghost` — прозрачная (для placeholder)

---

## 📊 Визуальные Состояния

### Карточка Может Быть

| Состояние | Эффекты |
|-----------|---------|
| **Normal** | scale: 1.0, opacity: 1.0 |
| **Hovered** | scale: 1.05, ring-1 primary/40 |
| **Focused** | scale: 1.1, ring-2 primary, shadow-2xl |
| **Related** | scale: 1.05, ring-1 primary/40, shadow-lg |
| **Dimmed** | opacity: 0.25, blur: 2px |

### Комбинации

**Focus Mode активен:**
- Focused card: elevated + ring-2 + shadow-2xl
- Related cards: subtle + ring-1 + shadow-lg
- Other cards: dimmed (opacity 0.25, blur 2px)

**Hover (без Focus Mode):**
- Hovered card: scale 1.05 + ring-1
- Related cards: ring-1
- Other cards: dimmed (opacity 0.25)

---

## 🚀 Использование

### Пример 1: Изучение Персоны

1. Откройте Timeline
2. Найдите персону (например, Аристотель)
3. **Кликните** → Focus Mode
4. **Увидите:**
   - Связанные работы (Метафизика, Этика)
   - Связанные концепции (Энтелехия, Форма)
   - Линии связей между ними
5. **Кликните на связанную работу** → переключитесь на неё

### Пример 2: Фильтрация по Домену

1. Нажмите "Философия" в фильтрах доменов
2. **Видите только** философские заметки
3. Нажмите "Искусство" дополнительно
4. **Видите** философию + искусство
5. **Кликните "Сбросить фильтры"** → всё вернётся

### Пример 3: Hover для Быстрого Обзора

1. **Наведите курсор** на карточку (без клика)
2. **Увидите** подсвеченные связанные заметки
3. **Наведите на другую** → подсветка обновится
4. **Без Focus Mode** → быстрая навигация

---

## 🎯 Shortcuts

| Клавиша | Действие |
|---------|----------|
| **Escape** | Выйти из Focus Mode |
| **Click (1)** | Активировать Focus Mode |
| **Click (2)** | Открыть заметку |
| **Click (bg)** | Выйти из Focus Mode |

---

## 📝 Доработки (Опционально)

### Идеи для Будущего

1. **Multi-focus:**
   - Shift+Click для выбора нескольких карточек
   - Показать пересечения связей

2. **Timeline Animations:**
   - Анимированный zoom при входе в Focus Mode
   - Parallax для связей

3. **Связи Разных Типов:**
   - Цветовая кодировка линий (person→work vs person→concept)
   - Толщина линии = сила связи

4. **Группы:**
   - Focus на эпоху → все заметки эпохи
   - Collapse/expand групп

5. **Search Integration:**
   - Ctrl+F → подсветить найденные заметки
   - Focus на результаты поиска

---

## ✨ Итоги

**Полностью реализовано:**
- ✅ Focus Mode с затемнением
- ✅ Связи между карточками (линии)
- ✅ Hover highlights
- ✅ Фильтры по доменам
- ✅ Плавные transitions
- ✅ Keyboard shortcuts

**Готово к использованию!** 🎉

---

## 🔗 Связанные Документы

- **TIME_RULER_USAGE.md** — основное руководство по Time Ruler
- **TIME_RULER_DESIGN.md** — архитектура и дизайн
- **PROJECT_SUMMARY.md** — общий обзор проекта

