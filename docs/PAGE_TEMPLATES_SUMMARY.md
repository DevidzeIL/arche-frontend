# Шаблоны страниц - Итоговое резюме

## ✅ Что сделано

### 1. Исправлена ошибка `getNoteById`
- Заменено на `getNote` в `NotePage.tsx`
- Теперь страницы открываются корректно

### 2. Созданы 5 шаблонов страниц

#### PersonPage (`src/components/pages/PersonPage.tsx`)
- **Hero**: Портрет + заголовок + краткое интро
- **Метаданные**: Годы жизни, место, национальность, профессия
- **Layout**: 3 колонки (портрет | текст | sidebar)
- **Sidebar**: Контекст, ключевые работы, ключевые идеи
- **Адаптив**: На мобильных - 1 колонка

#### WorkPage (`src/components/pages/WorkPage.tsx`)
- **Hero**: Заголовок + метаданные (год, техника, размеры)
- **Layout**: 2 колонки (текст | изображение sticky)
- **Изображение**: Крупное справа, подпись с деталями
- **Навигация**: Кнопки prev/next для навигации по работам
- **Адаптив**: На мобильных изображение сверху

#### ConceptPage (`src/components/pages/ConceptPage.tsx`)
- **Hero**: Заголовок + краткое определение (в рамке)
- **Layout**: 2 колонки (текст | sidebar)
- **Sidebar**: Ключевые вопросы, аргументы, контр-аргументы, происхождение
- **Адаптив**: На мобильных sidebar снизу

#### TimePage (`src/components/pages/TimePage.tsx`)
- **Hero**: Заголовок + диапазон лет
- **Суть эпохи**: 5 тезисов в grid (3 колонки)
- **Ключевые элементы**: Люди, работы, концепции (3 колонки)
- **Кнопка**: "На таймлайне" для перехода
- **Адаптив**: На мобильных grid → 1 колонка

#### Default (fallback)
- Простой шаблон для других типов заметок
- Базовый layout с markdown контентом

### 3. Парсер метаданных
- `src/components/pages/metadataParser.ts`
- Использует `gray-matter` для парсинга YAML frontmatter
- Автоматически определяет тип метаданных по `note.type`

### 4. Типы метаданных
- `src/components/pages/types.ts`
- Определены интерфейсы для всех типов метаданных
- TypeScript типизация для безопасности

### 5. Обновлён NotePage
- Автоматический выбор шаблона по типу заметки
- Парсинг метаданных из frontmatter
- Fallback для неизвестных типов

## 📋 Структура файлов

```
src/components/pages/
├── types.ts              # Типы метаданных
├── metadataParser.ts     # Парсер frontmatter
├── PersonPage.tsx        # Шаблон для персон
├── WorkPage.tsx          # Шаблон для работ
├── ConceptPage.tsx      # Шаблон для концепций
├── TimePage.tsx         # Шаблон для эпох
└── index.ts             # Экспорты
```

## 🎨 Дизайн-принципы

### Типографика
- **Заголовки**: Serif (Crimson Pro), font-light, 5xl-6xl
- **Текст**: Sans-serif (Inter), text-base, leading-relaxed
- **Максимальная ширина**: 65-75ch для читаемости
- **Межстрочный интервал**: 1.6-1.8

### Цвета
- Используются CSS переменные (light/dark theme)
- Muted colors для метаданных
- Primary для акцентов

### Адаптивность
- **Desktop (lg)**: Grid 3 колонки, sticky элементы
- **Tablet (md)**: Grid 2 колонки, адаптивные размеры
- **Mobile**: 1 колонка, вертикальный layout

## 📝 Примеры frontmatter

### Person
```yaml
---
type: person
birthYear: 1883
deathYear: 1924
portrait: image.jpg
keyWorks: [Работа 1, Работа 2]
---
```

### Work
```yaml
---
type: work
year: 1925
medium: Масло на холсте
image: painting.jpg
previousWork: note-id-1
nextWork: note-id-2
---
```

## 🚀 Использование

Страница автоматически выбирает шаблон:

```typescript
// В NotePage.tsx
switch (note.type) {
  case 'person': return <PersonPage ... />;
  case 'work': return <WorkPage ... />;
  // и т.д.
}
```

## ✅ Проверка

1. Откройте заметку типа `person` → должен показаться PersonPage
2. Откройте заметку типа `work` → должен показаться WorkPage
3. Проверьте адаптивность на разных размерах экрана
4. Проверьте, что метаданные из frontmatter отображаются

## 📚 Документация

Полная документация: `docs/PAGE_TEMPLATES_GUIDE.md`


