/**
 * @deprecated CardLayer больше не используется в TimeRuler
 * Вместо него используется RowsLayer → RowCardsLayer для семантических строк
 * 
 * Этот файл оставлен для справки, но не должен импортироваться в новых компонентах.
 * Планируется удаление в будущих версиях.
 */

// Весь код перенесен в RowCardsLayer.tsx

