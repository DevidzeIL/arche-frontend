/**
 * @deprecated useGreedyLayout больше не используется в TimeRuler
 * Вместо него используется bucketLayout для размещения карточек внутри строк
 * 
 * Этот файл оставлен для справки, но не должен импортироваться в новых компонентах.
 * Планируется удаление в будущих версиях.
 */

// Логика перенесена в bucketLayout.ts

